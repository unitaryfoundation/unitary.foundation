<div class="schedule">
  <slot />
</div>

<style>
  .schedule {
    @apply my-6 overflow-x-auto;
  }

  .schedule :global(table) {
    @apply w-full min-w-[32rem] border-collapse;
  }

  .schedule :global(thead) {
    @apply sr-only;
  }

  .schedule :global(td) {
    @apply border-b border-gray-300 px-3 py-3 text-left align-top;
  }

  .schedule :global(td:first-child) {
    @apply w-px whitespace-nowrap;
  }
</style>
